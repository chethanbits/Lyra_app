{
  "name": "lyra-align-your-day",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:android": "cross-env VITE_API_URL=http://127.0.0.1:8000 npm run build",
    "cap:sync": "npm run build && npx cap sync",
    "cap:sync:android": "npm run build:android && npx cap sync android",
    "cap:open:android": "npx cap open android",
    "android": "npm run cap:sync:android && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.1.0",
    "@capacitor/core": "^8.1.0",
    "motion": "^12.34.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.22.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0",
    "@vitejs/plugin-react": "^4.2.1",
    "cross-env": "^10.1.0",
    "vite": "^5.1.0"
  }
}
